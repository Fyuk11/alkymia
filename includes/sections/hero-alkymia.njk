<section id="hero" class="hero-premium">
  <div class="hero-premium__background">
    {% if section_data.background %}
      <img src="{{ section_data.background }}" alt="" class="hero-premium__bg-image">
    {% endif %}
    <div class="hero-premium__overlay"></div>
    <div class="hero-premium__gradient"></div>
    
    <!-- Contenedor de Partículas -->
    <div class="hero-premium__particles"></div>
  </div>
  
  <div class="container">
    <div class="hero-premium__content">
      <!-- Texto Principal -->
      <div class="hero-premium__text">
        <h1 class="hero-premium__title">
          {{ section_data.title | safe | replace('<span>', '<span class="text-gold">') }}
        </h1>
        
        <p class="hero-premium__subtitle">{{ section_data.subtitle }}</p>

        <!-- Stats de Confianza -->
        <div class="hero-premium__stats">
          <div class="stat-item">
            <span class="stat-number">+50</span>
            <span class="stat-label">Proyectos Entregados</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">+85%</span>
            <span class="stat-label">Tasa de Conversión</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">24/7</span>
            <span class="stat-label">Soporte Premium</span>
          </div>
        </div>

        <!-- CTAs -->
        {% if section_data.ctas %}
          <div class="hero-premium__actions">
            {% for cta in section_data.ctas %}
              <a href="{{ cta.url }}" class="btn btn--primary btn--large" target="_blank">
                <span>{{ cta.text }}</span>
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
                  <path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2"/>
                </svg>
              </a>
            {% endfor %}
            
            <!-- CTA Secundario -->
            <a href="#benefits" class="btn btn--secondary btn--large">
              <span>Conocer Beneficios</span>
            </a>
          </div>
        {% endif %}
      </div>

      <!-- Media -->
      <div class="hero-premium__media">
        {% if section_data.media %}
          <div class="media-container">
            <img src="{{ section_data.media }}" alt="Demo Alkymia Digital" class="hero-premium__image" loading="eager">
            
            <!-- Elementos Flotantes de Confianza -->
            <div class="floating-card floating-card--1">
              <div class="card-icon">🚀</div>
              <span>+85% Conversión</span>
            </div>
            <div class="floating-card floating-card--2">
              <div class="card-icon">⚡</div>
              <span>Performance Optimizada</span>
            </div>
            <div class="floating-card floating-card--3">
              <div class="card-icon">💰</div>
              <span>ROI Garantizado</span>
            </div>
          </div>
        {% endif %}
      </div>
    </div>
  </div>

  <!-- Scroll Indicator Elegante -->
  <div class="hero-premium__scroll">
    <span class="scroll-text">Descubre Más</span>
    <div class="scroll-arrow"></div>
  </div>
</section>

<!-- Incluir el JS específico del hero -->
<script type="module">
  import { initHeroAlkymia } from '/assets/js/modules/hero-alkymia.js';
  document.addEventListener('DOMContentLoaded', initHeroAlkymia);
</script>
<!-- sections/para-quien-es-v2.njk -->
<section id="para-quien-es" class="para-quien-es-v2">
  <div class="container">
    <div class="para-quien-es-v2__header">
      <h2 class="para-quien-es-v2__title">{{ section_data.title }}</h2>
      <p class="para-quien-es-v2__subtitle">{{ section_data.subtitle }}</p>
    </div>

    <div class="perfiles-grid">
      {% for perfil in section_data.perfiles %}
      <div class="perfil-card">
        <div class="perfil-header">
          <div class="perfil-icon">{{ perfil.icono }}</div>
          <h3 class="perfil-tipo">{{ perfil.tipo }}</h3>
        </div>
        
        <p class="perfil-descripcion">{{ perfil.descripcion }}</p>
        
        <div class="perfil-sintomas">
          <h4 class="sintomas-title">¿Te suenan estos síntomas?</h4>
          <ul class="sintomas-list">
            {% for sintoma in perfil.sintomas %}
            <li class="sintoma-item">
              <span class="sintoma-bullet">•</span>
              {{ sintoma }}
            </li>
            {% endfor %}
          </ul>
        </div>
        
        <div class="perfil-solucion">
          <div class="solucion-icon">💡</div>
          <div class="solucion-content">
            <strong>Nuestra solución:</strong>
            <span>{{ perfil.solucion }}</span>
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>